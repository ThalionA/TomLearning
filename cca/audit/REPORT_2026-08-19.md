# Audit — temporal CCA method · 2026-08-19

**Scope.** `cca/` temporal arm (running-state, 10 ms bins): the method layer in
`src/tom_cca/`, the five drivers, the analyze/figs scripts behind `HANDOFF.md` §1.
Goal stated by Theo: consolidate, simplify, document → modular, robust, portable to
another dataset with minimal edits.

**Status (updated 2026-08-19, later the same day) — the DEDUP passes are DONE** (four
commits `93028ae` → `374c0dd`, each verified like-for-like, 504 tests):

| done | candidates | evidence |
|---|---|---|
| ✅ P0 | 4.8 equivalence tests (`tests/test_dedup_equivalence.py`, 90 tests pinning verbatim copies of the old code) | pass |
| ✅ P1/P3 src | 2.1 `lagpairs.py` (one pairer, 5 live callers), 2.2 one `fdr_bh`, 2.3 `core.pca_fit_flat/pca_scores` (7→1), 2.4 `preprocess.cap_running_bins` (4→1), 2.5 `preprocess.load_running_session/RunningSession` (13 driver blocks→1, skip reasons), 2.7 `lagged.ifi_sides` | 414 old tests unchanged + 90 new |
| ✅ P1 scripts | 2.10 `config.PAIRS_TEMPORAL/PAIR_NAMES` (51→1), 3.1 `figstyle.save` → `config.FIGURES_DIR` + `FIGURE_MIRROR_DIR` (env `CCA_FIG_MIRROR`), 3.2 `scripts/_common.py` (no `sys.path` in live scripts), 3.3 `dataio.load_learning_points`, 3.4 `config.TEMPORAL` (+ `EXPECTED_LEARNERS`), 3.5 one FS idiom, 4.2 skip reasons, 4.4 driver `main()`s halved | 6 drivers: byte-identical CSVs on animal 36 (7 runs incl. `--epochs`); 8 analyze scripts: tracked `results/` unchanged; 11 figs scripts: 48/48 PNGs byte-identical |
| ✅ docs | 2.6 HANDOFF §2.4 corrected, HANDOFF §1, README quick-start, `__init__` rewritten (1.5) | — |
| ⏳ open | **§1 archive (Theo's call)**, 1.7 `subspace_window` swap, 2.8 nulls, 2.9 per-animal aggregation, 2.11 stats helpers in live analyze scripts, 3.6 loader contract, 4.1 `cc_heldout` rename, 4.3 config sidecar, 4.5/4.6, 4.7 `METHOD.md`, `pyproject.toml` | see §6 P2/P4/P5 |

Historical caps were KEPT as parser defaults in `run_lag_subspaces` / `run_lag_cosine`
(12 000 / 600) and `run_fixed_subspace` (600 per trial) so the CSVs reproduce; they are now
printed and exposed (`--max-samples 0 --max-per-trial 0` uncaps).

**Original status line: report only. Nothing moved, nothing edited.** Pick candidates by number; each
section is applied on its own, smallest blast radius first, tests after each, one commit
per section. Candidates marked ⚠ change numbers or names that downstream files read —
those need a like-for-like check before/after.

**Raw sweeps** (file:line lists behind every claim): `REPORT_2026-08-19_appendix_*.md`
in this folder. Items marked **[verified]** I re-read in the source myself; the rest come
from the sweeps and were spot-checked.

---

## 0. Measured baseline

| what | value |
|---|---|
| `src/tom_cca/` | 31 modules, 6 954 LOC |
| `scripts/` | 83 `.py` (16 828 LOC) + 3 `.sh` + 1 `.m` |
| `tests/` | 34 files, 5 501 LOC — **414 pass in 42 s** (`PYTHONPATH=src python -m pytest tests/ -q`) |
| On the temporal path (HANDOFF §1) | 9 src modules · 5 `run_*` · 7 `analyze_*` · ~11 `figs_*` · `figstyle` · 2 utils |
| **Not** on the temporal path | ~14 src modules (spatial / landmark / kCCA / the original Arm-A/B pipeline) · ~45 scripts |
| Orchestrator | none (scripts run by hand; `STATE.md` §6 lists the order) |
| Package install | none — every script does `sys.path.insert(0, …/src)` |

The temporal arm is ~⅓ of the code but 100 % of the live results. The other ⅔ is
superseded per `STATE.md` §2 ("retained for the record") and is what makes the tree hard
to read.

---

## 1. DEAD / OFF-PATH → `legacy/` (git mv, mirror path, never delete)

Vocabulary: *dead* = zero inbound refs from entry points or tests; *off-path* = live
code that the temporal method does not use (superseded arms).

| # | Files | Problem | Solution | Conf. |
|---|---|---|---|---|
| **1.1** | `src/tom_cca/{lagged_temporal,lagged_landmark,landmark_align,segments,surrogate,stage3,sweep,crosspair,prune_table,kernel_cca,kcca_window,trajectory,early_trials,subspace_stats}.py`; the `*_temporal` / `*_landmark` branches of `pipeline.py` and `analysis.py` | The original Arm-A/Arm-B pipeline (`run_temporal.py` → `pipeline.prepare_pair_temporal` → `analysis.analyse_pair_temporal` → `lagged_temporal.lag_curve`) is **not imported by any of the five live drivers** [verified: drivers import `lagged`, `lag_subspace`, `fixed_subspace`, `partial`, `dataio`, `config` only]. It carries its own lag pairer, LOTO-CV, per-segment circshift null — a parallel method that no current number uses. Same for the spatial (`stage3`, `sweep`, `surrogate`, `crosspair`), landmark (`landmark_align`, `prune_table`), kCCA, trajectory/early-trials modules. | `git mv` to `src/tom_cca/legacy/` (keep import paths working via the legacy scripts' own `sys.path`), or a sibling package `tom_cca_legacy`. `pipeline.py`/`analysis.py`: split the temporal/landmark branches out with them; what remains is the spatial Stage-2 prep. Decide arm-by-arm — **this is the single biggest simplification (~3 000 src LOC, ~45 scripts)**. | high for "off-path"; the *decision* to archive is Theo's |
| **1.2** | `scripts/plot_{common_units,ifi_fs,parcoords,partial,subspace_similarity}.py`, `make_figure_deck.py` | Zero references anywhere (name or outputs); last touched 2026-05-24 / 06-12; all read spatial-arm pkls; `figs_*` superseded them. | `git mv scripts/legacy/`. | high |
| **1.3** | `scripts/{run_temporal,run_partial,run_committed,run_stage2,run_stage3,summarise_sweep,plot_stage2,plot_stage3,plot_sweep_explore,plot_ifi_explore,committed_ifi,compare_nulls,run_review_batch.sh}` + all landmark (`summarise_landmark_sweep`, `plot_landmark`, `learning_changes*`, `build_landmark_index`, `build_prune_table`, `regen_all_landmark_figs.sh`) + all kCCA (`run_kcca*`, `analyze_kcca`, `figs_kcca`, `prototype_kcca`) + `prototype_continuous_pcca`, `run_early_trials`, `analyze_early_trials`, `figs_early_trials`, `run_trajectory*`, `analyze_trajectory*`, `figs_trajectory*`, `analyze_learning_vs_time`, `analyze_dims_as_n`, `figs_rotation_cc1`, `run_epochs`, `analyze_epochs`, `run_transition`, `figs_units`, `figs_report`, `figs_paired`, `figs_area_gini`, `figs_stats_tables`, `analyze_bin10_full`, `analyze_ifi`, `analyze_ifi_windows`, `figs_ifi_windows`, `run_ifi_windows`, `make_smoothed_deck` | Spatial/landmark/kCCA/early-temporal scripts; newest PROJECT_LOG mention < 2026-07-01 for all but `figs_report`/`run_trajectory`/`run_epochs`/`run_transition` (mentioned 07-28/29 as *superseded*). `STATE.md` §6 still lists several as the reproduce path for `bin10_tables.md`. | `git mv scripts/legacy/<arm>/`. **Caveat:** `analyze_bin10_full.py` + `figs_report.py` produce `bin10_tables.md` §A–H which `STATE.md` §3.0 still cites — keep those two live or re-point STATE. Needs Theo to say which of `bin10_tables.md` findings are still load-bearing. | med — bulk is safe; the `bin10_tables` chain needs a call |
| **1.4** | `src/tom_cca/segments.py:124` `segment_paired_indices` | Only caller is `tests/test_segments.py` [verified: no src/scripts caller]. | goes with 1.1 | high |
| **1.5** | `src/tom_cca/__init__.py` | Re-exports 13 of 30 modules; every temporal module is missing; docstring says "Spatial CCA pipeline". Works only because submodule import is implicit. | Rewrite after 1.1: list the temporal modules, one-paragraph method docstring. | high |
| 1.6 | `src/tom_cca/core.py`: `residualise`, `zscore_units`, `pca_fit`, `pca_transform`, `cca_cv`, `trial_folds`, `choose_k`, `numerical_rank`, `k_for_variance`, `cca_in_sample` | Spatial-arm only (`pipeline.py`, `analysis.py` callers). Temporal path uses only `cca_fit`, `cca_score`, `_cca_fit_svd` (test reference). | Move with the spatial arm, **or** keep `pca_fit`/`pca_transform` and make the drivers use them (see 2.3) — they are the only NaN-safe PCA in the repo. | med |
| 1.7 | `src/tom_cca/subspace_window.py` (289 LOC) | Live, but `run_lag_subspaces.py:204-207` calls `window_subspace` **only to read `n_sig`**; the other ~20 fields are discarded. Its `_lag_curve_perdim` is the one lag pairer that **crosses trial boundaries** (docstring caveat `:17-18`). | Replace that call with `lagged.perdim_significance(null_mode="dominant")` ⚠ (n_sig may change — like-for-like run on one animal-pair first), then archive the module. | med |

---

## 2. DRIFT RISK — same transform in ≥ 2 places

These are where a port would silently diverge. Deletion test applied: "if B vanished, do
callers move to A naturally?" — yes for every row unless noted.

| # | Transform | Implementations | Differ? | Solution | Conf. |
|---|---|---|---|---|---|
| **2.1** | **Within-trial lag pairing** `X[t]↔Y[t+L]` | **9** [verified 5]: `lagged.lag_slice:23` (3-D, spatial), `lagged._segment_lagged_pairs:145`, `lagged_temporal._segment_pairs:48`, `lagged_landmark._window_pairs:52`, `segments.segment_paired_indices:124`, `lag_subspace.segment_lag:48`, `subspace_window._lag_curve_perdim:138`, `fixed_subspace.variate_lag_curve:123`, `fixed_subspace.trial_lag_moments:356` | Arithmetic identical; **short-segment guards differ** (`n <= |L|+2` vs `|L| >= n` vs `<10 rows`); `subspace_window` crosses trials. `fixed_subspace.py:399` asserts `curve_from_moments` ≡ `variate_lag_curve` — **untested**. | One `lagpairs.py` (or in `core`): `pair_within_groups(X, Y, groups, lag, Z=None) → (Xp, Yp, Zx, Zy, g)` + `lag_moments`. Write the equivalence test on a shared fixture FIRST, then repoint the 5 live ones. | high |
| **2.2** | **BH-FDR** | **4**, byte-for-byte same algorithm [verified 3]: `paired_stats.fdr_bh:71`, `lagged.fdr_bh:323` (docstring says deliberate), `fixed_subspace._fdr_bh:340`, `scripts/learning_changes.py:123` | identical today | Keep `paired_stats.fdr_bh`; import it. Zero behaviour change. | high |
| **2.3** | **PCA → scores** | **7**: `core.pca_fit/pca_transform:102,123` (only NaN-safe one), `subspace_window._pca_fit/_scores:60,71`, `lag_subspace._scores:78`, `early_trials._pca_fit:40`, inline SVD `fixed_subspace.fit_fixed:91-95`, and **byte-identical** `_pca_scores` in `run_lag_curves.py:83` / `run_lag_cosine.py:82` (md5 match [verified]; `run_ifi_windows.py:51` differs) | same maths; only `core` drops non-finite rows | One `pca_scores(M, k, train_mask=None)` in the method layer; drivers import it. | high |
| **2.4** | **Sample cap** (`_capped_index`) | **4** [verified]: `run_lag_curves.py:89` (uncapped default, CLI-exposed, printed), `run_lag_cosine.py:88` ≡ `run_lag_subspaces.py:80` (md5 match; **hard-coded 12 000 / 600, no flag, not surfaced**), `run_fixed_subspace.py:58` (`_cap_bins`: per-trial 600 only — **selects a different sample**) | two selection rules | One `cap_running_bins(trial_ids, max_total, max_per_trial)` in `preprocess`; every driver exposes `--max-samples/--max-per-trial`; cap written as a CSV column / sidecar. Then decide whether `run_lag_subspaces`/`run_lag_cosine` should be uncapped like `run_lag_curves` (HANDOFF §2 says fine for lag geometry). | high |
| **2.5** | **Running-bin mask + per-area prep block** | `dataio.temporal_segments:624` (gap-closing, min-run, `Segment`s — used only by the archived pipeline) vs the inline one-liner `run = ~isnan(trial_idx) & (vel >= thr)` + `select_units` + `min_units` loop in **13 drivers** (`run_lag_curves.py:144-157`, `run_lag_subspaces.py:123`, `run_lag_cosine.py:139`, `run_fixed_subspace.py:96`, `run_cc_label_track.py:130`, …) | **Every live result uses the driver version**; HANDOFF §2 documents the driver version. The `Segment` machinery is the orphan. | One `load_running_session(animal, cfg, bin_ms, smooth_ms, include_fs) → Session(areas: dict[str, ndarray], trial_ids, vel, …)` in `dataio`/`preprocess`. Drivers shrink by ~40 lines each. | high |
| **2.6** | **Partial-out call sites** | 1 implementation (`partial.partial_out_cv:17`) but **4 leakage levels**: `run_lag_curves.py:168` & `run_lag_cosine.py:158` in-sample over all rows before a held-out curve [verified]; `lag_subspace.lag_sweep:171` per-fold; `run_fixed_subspace.py:140` fit-trials; `run_cc_label_track.py:165` all rows by design | **HANDOFF §2.4 ("train-only coefficients where the readout is cross-validated") is true only for `run_lag_subspaces`.** Leakage is mild (Z regression is unsupervised w.r.t. X–Y) but the doc overstates. | Either make the shared prep do per-fold partial-out everywhere ⚠ (numbers move slightly) or fix the sentence in HANDOFF. Recommend: fix the doc now, unify later with 2.5. | high |
| **2.7** | **IFI** | canonical `lagged.information_flow_index:42`; `perdim_ifi.curve_ifi:28` returns **NaN** where canonical returns **0.0** for empty sides; `perdim_ifi.ifi_windows_by_dim:159-162` re-derives the clipped means inline for `degenerate`; `run_ifi_windows.py:117` a third inline copy; `analyze_cc_label_track.py:73` vs `analyze_fixed_subspace.py:56` use different entry points on the same curve class | 0.0-vs-NaN semantics | Merge into one `ifi.py`: `ifi(lags, cc) → (value, degenerate)`; `ifi_windows`; delete the inline copies. ⚠ NaN/0 choice must be made explicit and checked against the two analyze scripts. | high |
| 2.8 | **Null distributions** | 8 implementations; **3 live**: `lagged.perdim_significance:287` (global `np.roll`, min shift 1), `fixed_subspace.frozen_perm_null:323` (same), `subspace_window._significance:132` (in-sample, no p) [verified first two]. `cfg.temporal_circshift_min_bins=5` (`config.py:213`) is **read by none of them** (dead config). | Global roll over concatenated running bins is a valid lag-0 surrogate at n≈3e5 (P(shift<50 bins) ≈ 2e-4) — **not a result-changing bug**; it is a config/doc drift and three constructions. | One `circshift_null(Sx, Sy, groups, …)` with an explicit `min_shift` honouring the config; `perdim_significance` and `frozen_perm_null` call it. Archive the rest with 1.1. | med |
| 2.9 | **Per-animal aggregation** | `cc_aggregate.per_animal_mean:26` (sig gate, degenerate drop, duplicate guard) vs `analyze_cc_label_track._per_animal:79` (`pivot_table`, **none of the three guards**) vs hand-indexing in `analyze_fixed_subspace.py:74-87`, `analyze_lag_subspaces._paired:46`; `min_n` 3 in `cc_aggregate` vs `>= 2` in the three scripts | guards differ | Route all three through `cc_aggregate` ⚠ (n per pair may drop by one animal in places — report the before/after table). | high |
| 2.10 | **`PAIRS`** | `config.PAIRS` (tuples, one order) vs **15 drivers** re-declaring tuples vs **~35 analyze/figs** re-declaring `"CA1-RSC"` strings (different order); `prototype_kcca` a 5-pair subset | order differs; no drift in membership | `config.PAIRS` + `config.PAIR_NAMES` (strings, figure order); one import. | high |
| 2.11 | **Script-level stats helpers** | `_paired`×5, `_wilcoxon_vs0`×7, star formatters ×9 (`figstyle.star` exists), `_sem`×8, 13 local CSV `load()`s, 23 direct `ttest_rel/wilcoxon` calls — mostly in the off-path scripts | | Falls away with 1.2/1.3; for the live analyze scripts, import `paired_stats`/`figstyle.star`. | med |

---

## 3. PATHS & DATASET COUPLING (portability)

| # | Files | Problem | Solution | Conf. |
|---|---|---|---|---|
| **3.1** | 7 scripts hard-code `/Users/theoamvr/Documents/ResearchVault/attachments` (`figs_report.py:35`, `figs_units.py:37`, `figs_early_trials.py:29`, `figs_ifi_windows.py:25`, `figs_kcca.py:34`, `figs_rotation_cc1.py:30`, `analyze_epochs.py:32`) + `run_review_batch.sh:6`; 17 more use `Path.home()/"Documents"/…`; **26 figs scripts write to the vault as primary, `figstyle.save` mirrors to `cca/figures/`** | Unportable on any other machine/user; two spellings of one path; `config.FIGURES_DIR` exists and is bypassed. | `figstyle.save` owns the destination: default `config.FIGURES_DIR`, optional mirror from env var `CCA_FIG_MIRROR` (or a `config.FIGURE_MIRROR = None`). Scripts stop naming `ATT`. | high |
| **3.2** | 20 analyze/figs scripts: `RES = Path(__file__).resolve().parents[1] / "results"`; ~80 scripts: `sys.path.insert(0, …/"src")` | `config.RESULTS_DIR` exists; no package install. | Add `pyproject.toml` (`uv`, editable install) → no `sys.path` hacks; `from tom_cca import config` → `config.RESULTS_DIR`. One `scripts/_common.py` with `results_path(stem, fs, bin_ms)`. | high |
| **3.3** | `"animal_behaviour.mat"` re-spelled in 16 drivers calling private `dataio._read_behaviour_file`; `config.LEARNING_FILE` defined, **never referenced**; `run_transition.py:49` / `run_kcca_transition.py:47` open h5py keys (`world_binned`) directly | The data contract leaks out of `dataio`. | `dataio.load_learning_points(data_dir=None)` public; drivers call it. | high |
| **3.4** | Magic numbers: `K=30` in 11 scripts + 9 src defaults (`cfg.k_cap=30` exists); `--bin-ms` defaults **10 / 25 / 50** across drivers while `config.temporal_bin_ms=50` says "Locked at 50 ms" [verified `config.py:185-186`]; `--smooth-ms` default 2.5 in 2 drivers, 0.0 in 5 (usage text says 2.5); `N_FOLDS=5` ×12 (2 use 4); shuffles 200 vs `SURROGATE_SHUFFLES=100` vs 30/20/10; `alpha=0.05` ×20 never in config; `MAX_LAG` 25/10/8/4; `LABEL_W=5` twice with a "must match" comment (`run_cc_label_track.py:65`, `analyze_cc_label_track.py:48`); `BIN_MS=10` ×3; `FDR_DIMS=10` ×3; `EXPECTED_ANIMALS=12` ×2 | The canonical temporal config (10 ms, σ 2.5, k 30, 5 folds, 200 shuffles, ±250 ms, FDR over 10 dims, label window 5) **exists nowhere as one object** — it is reconstructed per script. Config's own temporal defaults are wrong for the live method. | A frozen `TemporalConfig` dataclass (in `config.py`) with those fields as the single default; drivers take `--config` overrides; every results CSV gets a sidecar `*.json` of the config used (also fixes the "cap not surfaced" smell). | high |
| **3.5** | `HCV1_` prefix in 26 figs scripts; `bin10` baked into 5 figs scripts' input/output stems (`figs_cc_label_track.py:100-217`, `figs_cc_ifi_signs.py:154-213`, `figs_perdim_ifi.py:105-149`, `figs_lag_subspaces_epochs.py:90-134`, `analyze_bin10_full.py:14`); `_fsincl` suffix handled in **two incompatible idioms** (drivers: `--include-fs`; readers: `"fsincl" in sys.argv[1:]`) with **one polarity inversion** (`figs_area_gini.py:64` defaults FS-*included*) | Dataset name + bin width + FS condition are filename conventions, not parameters. | `config.FIG_PREFIX` (default `"HCV1"`), `results_path()` / `fig_path()` helpers that take `(stem, bin_ms, fs)`; one `--include-fs` flag everywhere via the shared parser. | high |
| 3.6 | `dataio.py` (.mat keys `:80-577`), `config.py` (areas/pairs/FS areas/paths) | This is the *right* place for dataset coupling — **`src/` is otherwise clean: zero area-name literals outside `config.py`** [sweep]. But the learning-point/epoch design (`dataio.classify_cohort:369`, `epoch_windows:400`, `EPOCH_NAMES` iterated in `pipeline.py:381,398`) is in the method layer, and `fixed_subspace.band_occupancy:255` defaults to 5–12 Hz theta with a dataset-specific docstring. | Portability = a documented **loader contract**: "a `Session` = spikes 1 ms `(T, n_units)`, velocity `(T,)`, trial index `(T,)`, `area_masks`, `fs_mask`, `learning_point`". New dataset ⇒ write one loader returning that; nothing downstream changes. Epoch definition becomes a config callable. | med |
| 3.7 | `tests/test_dataio_temporal.py:96` depends on `config.DATA_DIR/"TF028_export.mat"` (skips if absent) | Fine (skips), but ties the suite to this machine. | Keep; note in README that 2 tests need the real data. | low |

---

## 4. SMELLS (robustness + documentation)

| # | Files | Problem | Solution | Conf. |
|---|---|---|---|---|
| **4.1** | `scripts/run_cc_label_track.py:255` → column **`cc_heldout`** = `frozen_perm_null(...).r_obs`, which is the **in-sample** canonical r of the frozen fit [verified]; `HANDOFF.md:102` says "`cc_heldout` from `frozen_perm_null`" (true but misleading); consumed by `analyze_cc_label_track.py:71` (carried, not tested on) | Mislabelled column; a future reader will treat it as a coupling strength. | Rename to `r_frozen` (or `cc_insample`) ⚠ + update HANDOFF §3 + the one consumer. | high |
| **4.2** | Driver `except Exception: continue` around stream loading (`run_lag_curves.py:140-143`, `run_lag_subspaces.py:119-122`, `run_lag_cosine.py:135-138`, `run_fixed_subspace.py:92-95`, `run_cc_label_track.py:126-129`); `run_lag_subspaces.py:208-209` turns a failed `window_subspace` into `n_sig = 0` written as a real value; `lagged.py:296` a failed surrogate becomes an empty row and the p denominator shifts | An animal can vanish from the cohort, or a failure becomes a zero, with only a print. | Log to a `*_skipped.csv` sidecar with the reason; never write a failure as a value (write NaN + `status` column). Part of the shared prep (2.5). | high |
| **4.3** | Silent defaults never in outputs: caps (2.4), `_COV_EIG_FLOOR=1e-10` (`core.py:142`, truncates d without warning), `min_units`, `velocity_thresh`, `FDR_DIMS`, `LEAD_DIMS=5`, `far_ms=200`, `TAU_BINS=5`, `NOT_ESTIMABLE_DEG=70` | Not reproducible from the CSV alone. | Config sidecar (3.4); `cca_fit` returns the rank it kept. | high |
| **4.4** | `main()` > 100 lines: `run_cc_label_track.py:95` (170), `run_lag_cosine.py:111` (162), `run_lag_curves.py:110`, `run_fixed_subspace.py:66`, `analyze_cc_ifi_signs.py:479` | load → mask → residualise → PCA → fit → null → rows all inline; this is *why* 2.1–2.6 exist. | After 2.5 each driver is: `for session, pair: S = prepare(...); out = method(S, cfg); write(out)`. | high |
| 4.5 | Hard-coded verdict strings: `analyze_fixed_subspace._md_ringing:197-202` (`"integration window"`/`"half-period of a rhythm"` from a fixed 0.5 threshold); `analyze_lag_subspaces.stability:57-74` docstring carries a numeric verdict | Same trap as the 08-17 `_mixing_verdict` fix (GOTCHAS). | Compute from the data, or drop the prose from code. | med |
| 4.6 | Numerical: IFI `0.0` means both "balanced" and "no coupling" (`lagged.py:49-50`) — disambiguated only in `perdim_ifi.ifi_windows_by_dim`, not in `ifi_by_window`/`run_ifi_windows`; `analyze_cc_crosscorr_epochs.curve_metrics:145-147` `peak = max(r)` with no positivity guard; `paired_t` allows n=2 while `cc_aggregate` enforces 3; `subspace_window._significance:131` `integers(1, n)` raises at n≤1 | | Fold into 2.7 (IFI returns `degenerate`), 2.9 (`min_n`), and the `curve_metrics` guard. | med |
| 4.7 | **Docs out of date**: `README.md` (109 tests, spatial-first, `temporal_bin_ms` 50, no mention of the five drivers), `config.py:185` "Locked at 50 ms", `UNDERSTANDING_temporal.md` §8 describes the archived pipeline as the architecture, `HANDOFF.md` §2.4 (see 2.6), `__init__` docstring, `STATE.md` §6 reproduce block lists superseded scripts | A cold reader (or a port) gets the archived method. | One **`METHOD.md`** (≤ 2 pages): the temporal method step-by-step, each step naming the one function that implements it + its config field + its test; README points there; HANDOFF keeps the file/column map. | high |
| 4.8 | Tests: no equivalence test across the lag pairers (2.1), the `fdr_bh` copies (2.2), `curve_from_moments` vs `variate_lag_curve` (`fixed_subspace.py:399` asserts it), or any `_capped_index` | The consolidation in §2 needs these as its safety net. | Write them **first** (TDD per CLAUDE.md) — they pass on the current copies, then guard the merge. | high |

---

## 5. Target shape (for discussion — recommendation first)

Keep the package name; no rename churn. The method layer becomes **eight files on the
temporal path**, everything else under `legacy/`:

```
src/tom_cca/
  config.py        dataset contract (AREAS, PAIRS, FS_AREAS, paths) + TemporalConfig (10 ms, σ2.5, k30, 5 folds, 200 shuf, ±250 ms, fdr_dims 10, label_w 5)
  dataio.py        THIS dataset's loaders → a neutral Session (spikes/vel/trial_idx/area_masks/fs_mask/LP). Porting = rewrite this file only.
  preprocess.py    NEW: bin+smooth, running mask, unit selection, partial-out, pca_scores, cap  (today: copied in 13 drivers)
  lagpairs.py      NEW: the ONE within-group pairer + lag moments                              (today: 9 copies)
  core.py          cca_fit / cca_score (+ _cca_fit_svd reference)                              (spatial helpers → legacy)
  nulls.py         perdim_significance, frozen_perm_null, circshift_null, fdr_bh (one)         (today: lagged + fixed_subspace + paired_stats)
  ifi.py           information_flow_index(+degenerate), ifi_windows, perdim helpers            (today: lagged + perdim_ifi + 2 inline)
  stats.py         paired_stats + cc_aggregate
  lag_subspace.py, fixed_subspace.py   the two families, thinned to use the above
  legacy/          spatial · landmark · kcca · arm-AB pipeline (git mv, tests move with them)
scripts/
  _common.py       parent argparser (--bin-ms --smooth-ms --include-fs --max-samples …), results_path/fig_path, PAIRS from config
  run_{lag_curves,lag_subspaces,lag_cosine,fixed_subspace,cc_label_track}.py   ~60 lines each
  analyze_*.py ×7, figs_*.py ×11, figstyle.py, merge_lag_curve_parts.py
  legacy/          everything in §1.2–1.3
pyproject.toml     uv-managed, editable install → no sys.path hacks
METHOD.md          the method, step → function → config field → test
```

Alternatives considered: (b) leave layout, only de-duplicate in place — cheaper (half the
work) but the tree stays 3× larger than the method and the next reader still can't tell
live from superseded; (c) new repo for the temporal method — cleanest for porting but
orphans the logs/GOTCHAS that make the results trustworthy. **(a) recommended.**

---

## 6. Suggested passes (smallest blast radius first; one commit each; 414 tests after each)

| pass | candidates | behaviour change | est. |
|---|---|---|---|
| **P0** equivalence tests | 4.8 | none | ~1 h |
| **P1** zero-change de-dup | 2.2, 2.3, 2.10, 3.1, 3.2 (paths + `pyproject`), 3.3 | none (numbers identical by construction; diff the CSV of one animal-pair) | ~half a day |
| **P2** archive | 1.1–1.6 (+1.7 after its like-for-like) | none to live outputs; `STATE.md` §6 re-pointed | ~2 h + Theo's arm-by-arm decision |
| **P3** shared prep + lagpairs | 2.1, 2.4, 2.5, 4.2, 4.4 | none if P0 tests hold; cap exposure changes *nothing* until a default is changed | ~1 day |
| **P4** ⚠ semantic fixes | 2.6 (doc first), 2.7, 2.8, 2.9, 4.1, 4.5, 4.6 | small, enumerated; each gets a before/after table in the log | ~half a day |
| **P5** config object + sidecars + docs | 3.4, 3.5, 3.6, 4.3, 4.7 | none; adds files | ~half a day |

Total ≈ 3 days of agent time with review points after each pass. P1+P2 alone remove
~⅔ of the visible tree with zero risk to any number.

---

## 7. Not checked (so you know what this report does not cover)

- Whether any *result CSV* in `results/` (389 tracked) is orphaned — provenance by file
  was not swept; §1 is by script/module references only.
- Numerical agreement of the 9 lag pairers on real data — only their code was compared.
- The spatial/landmark arms' internal quality — out of scope; they are archived as-is.
- Timing of the five drivers after consolidation — unmeasured; no reason to expect change.
